import { Component } from "react";
import { connect } from 'react-redux'



class MainCnfgContainers extends Component {

}

const mapStateToProps = (state, ownProps) => {
    if (ownProps.NewContainerId === -1) {

    }
    state.MaxNewContainer
    return (
        {

        }
    )
}

export default connect(mapStateToProps, mapDispathToProps)(MainCnfgContainers)